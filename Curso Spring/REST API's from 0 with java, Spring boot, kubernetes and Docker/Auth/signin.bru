meta {
  name: signin
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/signin
  body: json
  auth: inherit
}

body:json {
  {
    "username": "Romulo Perirara",
    "password": "admin123"
  }
}

script:post-response {
  let data = res.body;
  bru.setEnvVar("token", res.body.accessToken);
  bru.setEnvVar('refreshToken', res.body.refreshToken);
}

settings {
  encodeUrl: true
  timeout: 0
}
